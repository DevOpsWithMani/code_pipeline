version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
    overwrite: true
hooks:
  BeforeInstall:
    - location: inline
      timeout: 300
      runas: root
      command: |
        rm -rf /var/www/html/*
        mkdir -p /var/www/html
  ApplicationStart:
    - location: inline
      timeout: 300
      runas: root
      command: |
        systemctl restart apache2.service
